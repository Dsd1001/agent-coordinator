{
  "name": "agent-coordinator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transport-neutral manager/worker agent coordination with durable state, isolated workers, recovery, quotas, and extensions",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "release-check": "sh scripts/release-check.sh"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^5.9.2"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Dsd1001/agent-coordinator.git"
  },
  "bugs": {
    "url": "https://github.com/Dsd1001/agent-coordinator/issues"
  },
  "homepage": "https://github.com/Dsd1001/agent-coordinator#readme",
  "engines": {
    "node": ">=22.13.0"
  }
}
